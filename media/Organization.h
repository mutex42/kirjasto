#ifndef ORGANIZATION_H
#define ORGANIZATION_H

#include <string>

using namespace std;

class Organization
{
public:
    string name;
    Organization(string);
    ~Organization();
    Organization();
};

#endif // ORGANIZATION_H
